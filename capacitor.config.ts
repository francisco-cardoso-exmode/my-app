import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.expertmode.com',
  appName: 'my-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
